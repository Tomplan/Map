[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  VITE_DEFAULT_PATH = "/admin"
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
