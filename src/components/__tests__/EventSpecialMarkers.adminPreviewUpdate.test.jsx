// Placeholder test — the more invasive admin preview test was reverted because
// it caused markers to disappear in the UI for some users. Keep a harmless
// placeholder so CI remains stable while we follow up on a safer implementation.
test('placeholder — EventSpecialMarkers admin preview change neutralized', () => {
  expect(true).toBe(true);
});
